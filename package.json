{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "typecheck": "tsc --build",
    "build": "pnpm --filter @workspace/www-studio run build && cp -r api artifacts/www-studio/dist/public/",
    "kb:seed": "tsx scripts/seed-kb.ts"
  },
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}